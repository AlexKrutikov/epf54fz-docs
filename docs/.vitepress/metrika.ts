// Номер счётчика Яндекс Метрики.
// Берётся из переменной окружения YM_COUNTER_ID (можно задать при сборке),
// иначе — значение по умолчанию ниже. Замените на номер своего счётчика.
export const YM_COUNTER_ID = Number(process.env.YM_COUNTER_ID) || 109756539
