{
  // Paste Image (mushan.vscode-paste-image): Ctrl+Alt+V в открытом .md
  // сохраняет скриншот из буфера обмена в docs/public/images/<имя-страницы>/
  // и вставляет в текст ссылку ![](/images/<имя-страницы>/<файл>.png) —
  // формат public-путей VitePress. Перед сохранением показывается путь,
  // его можно поправить (другая подпапка, говорящее имя файла).
  // Встроенную вставку/перетаскивание картинок VS Code отключаем: она кладёт
  // image.png рядом с .md и вставляет относительную ссылку ![alt text](image.png),
  // что ломает конвенцию public-путей VitePress. Вставка — только Ctrl+Alt+V (Paste Image).
  // Если хоткей перестал срабатывать — проверить, не занял ли его глобальный перехватчик
  // (Punto Switcher уже занимал, освобождён 31.08.2026).
  "markdown.editor.filePaste.enabled": false,
  "markdown.editor.filePaste.copyIntoWorkspace": "never",
  "markdown.editor.drop.enabled": false,
  "markdown.editor.drop.copyIntoWorkspace": "never",

  "pasteImage.path": "${projectRoot}/docs/public/images/${currentFileNameWithoutExt}",
  "pasteImage.basePath": "${projectRoot}/docs/public",
  "pasteImage.prefix": "/",
  "pasteImage.forceUnixStyleSeparator": true,
  "pasteImage.defaultName": "Y-MM-DD_HH-mm-ss",
  "pasteImage.showFilePathConfirmInputBox": true,
  "pasteImage.filePathConfirmInputBoxMode": "fullPath",
  "pasteImage.insertPattern": "![](${imageFilePath})"
}
